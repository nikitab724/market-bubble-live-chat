# components AGENTS.md

This directory owns shadcn-style reusable React components.

Rules:
- Put registry-style primitives in `components/ui/`.
- Keep app-specific layout in `src/ui/` unless a component is meant to be reusable.
- Prefer Tailwind utility classes and shared helpers from `lib/`.
