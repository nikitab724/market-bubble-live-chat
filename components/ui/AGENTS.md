# components/ui AGENTS.md

This directory owns reusable UI primitives that follow the shadcn import convention.

Rules:
- Export components by name from their own files.
- Keep demos small and colocated only when they are useful examples.
- Use `@/lib/utils` for class merging.
